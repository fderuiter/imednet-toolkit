name: Pipeline

on:
  push:
    branches: [main]
    tags:
      - "imednet-v*.*.*"
      - "imednet-workflows-v*.*.*"
      - "apache-airflow-providers-imednet-v*.*.*"
      - "imednet-streamlit-v*.*.*"
      - "imednet-plugins-sinks-v*.*.*"
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  # ------------------------------------------------------------------
  # JOB 0: PR TITLE LINT (Pull requests only)
  # ------------------------------------------------------------------
  semantic-pr-title:
    name: Semantic PR Title
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    steps:
      - name: Validate PR title
        if: ${{ !contains(github.event.pull_request.user.login, '[bot]') }}
        uses: amannn/action-semantic-pull-request@v6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          types: |
            feat
            fix
            chore
            docs
            ci
            test
            refactor
            perf
            revert

  # ------------------------------------------------------------------
  # JOB 1: QUALITY GATE (Runs per package)
  # ------------------------------------------------------------------
  # ------------------------------------------------------------------
  # JOB 0.1: GLOBAL LINTING (Runs unconditionally)
  # ------------------------------------------------------------------
  global-lint:
    name: Global Code Quality Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: pip install ruff pylint
      - name: Run Ruff Linter
        run: ruff check . --select F401,F841
      - name: Run Similarity Check
        run: pylint --disable=all --enable=similarities --min-similarity-lines=4 $(git ls-files 'packages/**/*.py')

  # ------------------------------------------------------------------
  # JOB 0.5: DETECT CHANGES & PLAN
  # ------------------------------------------------------------------
  plan:
    name: Plan CI Execution
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
      build_matrix: ${{ steps.set-matrix.outputs.build_matrix }}
      run_integration: ${{ steps.filter.outputs.export_logic }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            export_logic:
              - 'packages/**/integrations/**'
              - 'packages/**/tests/integration/**'
              - 'docker-compose.yml'

      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v46
        with:
          separator: ","

      - name: Determine Impacted Packages
        id: set-matrix
        run: |
          if [ -z "${{ steps.changed-files.outputs.all_changed_files }}" ]; then
            PLAN=$(echo "" | python3 scripts/ci_plan.py)
          else
            PLAN=$(echo "${{ steps.changed-files.outputs.all_changed_files }}" | tr ',' '\n' | python3 scripts/ci_plan.py)
          fi
          echo "Impacted packages: $PLAN"
          echo "matrix=$(echo "$PLAN" | jq -c '.test')" >> "$GITHUB_OUTPUT"
          echo "build_matrix=$(echo "$PLAN" | jq -c '.build')" >> "$GITHUB_OUTPUT"

  scorecard:
    name: OpenSSF Scorecard
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      id-token: write
      contents: read
      actions: read
    steps:
      - name: "Checkout code"
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: "Run analysis"
        uses: ossf/scorecard-action@v2.4.4
        with:
          results_file: results.sarif
          results_format: sarif
          publish_results: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}

      - name: "Upload artifact"
        uses: actions/upload-artifact@v4
        with:
          name: SARIF file
          path: results.sarif
          retention-days: 5

      - name: "Upload to code-scanning"
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results.sarif

  quality:
    name: Workspace Quality & Security
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Cache Workspace Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/uv
            .venv
          key: uv-venv-${{ runner.os }}-"3.10"-${{ hashFiles('uv.lock') }}
          restore-keys: |
            uv-venv-${{ runner.os }}-"3.10"-
      - name: Install Hatch
        run: python -m pip install --upgrade pip hatch

      - name: Run Quality & Security Checks
        run: hatch run quality:all packages

  # ------------------------------------------------------------------
  # JOB 2: BUILD ARTIFACT (Builds the library)
  # ------------------------------------------------------------------
  build:
    name: Build Distribution
    needs: [plan, quality]
    if: needs.plan.outputs.matrix != '[]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Cache Workspace Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/uv
            .venv
          key: uv-venv-${{ runner.os }}-"3.10"-${{ hashFiles('uv.lock') }}
          restore-keys: |
            uv-venv-${{ runner.os }}-"3.10"-
      - name: Install Dependencies
        run: |
          python -m pip install uv hatch
          uv sync --locked --all-extras
          echo "$GITHUB_WORKSPACE/.venv/bin" >> $GITHUB_PATH

      - name: Tighten Internal Dependencies
        run: python scripts/tighten_dependencies.py --rewrite

      - name: Build Workspace Packages
        run: |
          IMPACTED=$(echo '${{ needs.plan.outputs.build_matrix }}' | jq -r '.[]')
          for package in packages/*; do
            if [ -f "$package/pyproject.toml" ]; then
              PKG_NAME=$(basename "$package")
              if echo "$IMPACTED" | grep -qx "$PKG_NAME"; then
                (
                  cd "$package"
                  hatch build
                )
              else
                echo "Skipping $PKG_NAME as it is not impacted by this change plan."
              fi
            fi
          done

      - name: Upload Core Artifact
        if: contains(fromJSON(needs.plan.outputs.build_matrix), 'core')
        uses: actions/upload-artifact@v4
        with:
          name: dist-core
          path: packages/core/dist/
          retention-days: 1

      - name: Upload Workflows Artifact
        if: contains(fromJSON(needs.plan.outputs.build_matrix), 'plugins-workflows')
        uses: actions/upload-artifact@v4
        with:
          name: dist-workflows
          path: packages/plugins-workflows/dist/
          retention-days: 1

      - name: Upload Providers Artifact
        if: contains(fromJSON(needs.plan.outputs.build_matrix), 'providers-airflow')
        uses: actions/upload-artifact@v4
        with:
          name: dist-providers
          path: packages/providers-airflow/dist/
          retention-days: 1

      - name: Upload Sinks Artifact
        if: contains(fromJSON(needs.plan.outputs.build_matrix), 'plugins-sinks')
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: dist-sinks
          path: packages/plugins-sinks/dist/
          retention-days: 1

      - name: Upload Streamlit Artifact
        if: contains(fromJSON(needs.plan.outputs.build_matrix), 'plugins-streamlit')
        uses: actions/upload-artifact@v4
        with:
          name: dist-streamlit
          path: packages/plugins-streamlit/dist/
          retention-days: 1

      - name: Restore Internal Dependencies
        if: always()
        run: python scripts/tighten_dependencies.py --restore

  # ------------------------------------------------------------------
  # JOB 3: TEST MATRIX (Tests impacted packages)
  # ------------------------------------------------------------------
  test:
    name: Test ${{ matrix.package }} (Python ${{ matrix.python-version }})
    needs: [plan, build]
    if: needs.plan.outputs.matrix != '[]'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        package: ${{ fromJSON(needs.plan.outputs.matrix) }}
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache Workspace Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/uv
            .venv
          key: uv-venv-${{ runner.os }}-${{ matrix.python-version }}-${{ hashFiles('uv.lock') }}
          restore-keys: |
            uv-venv-${{ runner.os }}-${{ matrix.python-version }}-

      - name: Download Built Artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: dist-*
          path: dist
          merge-multiple: true

      - name: Install Dependencies
        run: |
          python -m pip install uv hatch
          uv sync --locked --all-extras
          echo "$GITHUB_WORKSPACE/.venv/bin" >> $GITHUB_PATH

          if ls dist/*.whl 1> /dev/null 2>&1; then
            uv pip install --force-reinstall --no-deps dist/*.whl
          fi

      - name: Run Tests
        env:
          IMEDNET_API_KEY: dummy
          IMEDNET_SECURITY_KEY: dummy
          IMEDNET_POSTMAN_PATH: ${{ github.workspace }}/imednet.postman_collection.json
        run: |
          if [ "${{ matrix.package }}" = "core" ]; then
            TEST_ARGS="--ignore-glob=*airflow* --ignore-glob=*workflows* --ignore-glob=*streamlit* --ignore-glob=*sinks* --ignore-glob=*browser* tests/"
            COV_ARGS="--cov=imednet"
          elif [ "${{ matrix.package }}" = "plugins-workflows" ]; then


            TEST_ARGS="tests/unit/test_workflows*.py tests/unit/workflows/ tests/workflows/ tests/unit/test_chunked_pipeline.py tests/unit/cli/ tests/unit/test_config_version_control.py tests/unit/test_duckdb_ingestion_workflow.py tests/unit/test_job_poller.py tests/unit/test_record_mapper.py tests/unit/test_record_mapper_hierarchy.py tests/unit/test_state_ledger.py tests/unit/test_study_structure.py tests/unit/test_sync_worker.py tests/unit/test_cached_records_loader.py"
            COV_ARGS="--cov=imednet_workflows"
          elif [ "${{ matrix.package }}" = "providers-airflow" ]; then
            TEST_ARGS="tests/unit/test_airflow*.py"
            COV_ARGS="--cov=apache_airflow_providers_imednet"
          elif [ "${{ matrix.package }}" = "plugins-streamlit" ]; then
            TEST_ARGS="tests/unit/test_streamlit*.py tests/unit/streamlit/ tests/unit/streamlit_plugin/"
            COV_ARGS="--cov=imednet_streamlit"
          elif [ "${{ matrix.package }}" = "plugins-sinks" ]; then
            TEST_ARGS="packages/plugins-sinks/tests/"
            COV_ARGS="--cov=imednet_sinks"
          fi

          hatch run test -q $COV_ARGS --cov-report=xml --cov-fail-under=90 -m "not live and not browser" --ignore=tests/live/ --ignore=tests/browser/ $TEST_ARGS || {
            RET=$?
            if [ $RET -eq 5 ]; then
              echo "No tests collected for ${{ matrix.package }}. Passing gracefully."
              exit 0
            else
              exit $RET
            fi
          }

  # ------------------------------------------------------------------
  # JOB 3.5: INTEGRATION SANDBOX (Hermetic testing for localizable sinks)
  # ------------------------------------------------------------------
  integration-sandbox:
    name: Hermetic Integration Sandbox
    needs: [plan, build]
    if: needs.plan.outputs.run_integration == 'true' && needs.plan.outputs.matrix != '[]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Cache Workspace Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/uv
            .venv
          key: uv-venv-${{ runner.os }}-"3.10"-${{ hashFiles('uv.lock') }}
          restore-keys: |
            uv-venv-${{ runner.os }}-"3.10"-

      - name: Install Dependencies
        run: |
          python -m pip install uv hatch
          uv sync --locked --all-extras
          echo "$GITHUB_WORKSPACE/.venv/bin" >> $GITHUB_PATH

      - name: Run Hermetic Integration Sandbox
        env:
          IMEDNET_API_KEY: dummy
          IMEDNET_SECURITY_KEY: dummy
        run: |
          echo "Starting ephemeral containerized databases..."
          docker compose up -d
          echo "Waiting for databases to initialize..."
          sleep 15
          echo "Running integration tests..."
          IMEDNET_TEST_CONTAINERS=1 pytest packages/plugins-sinks/tests/integration/test_containerized_sinks.py -v || { status=$?; docker compose down -v; exit $status; }
          echo "Cleaning up containers..."
          docker compose down -v

  # ------------------------------------------------------------------
  # JOB 3.6: SECURITY ROBUSTNESS (Fuzzing)
  # ------------------------------------------------------------------
  fuzzing:
    name: Robustness Fuzzing
    needs: build
    if: needs.plan.outputs.matrix != '[]'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Cache Workspace Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/uv
            .venv
          key: uv-venv-${{ runner.os }}-"3.12"-${{ hashFiles('uv.lock') }}
          restore-keys: |
            uv-venv-${{ runner.os }}-"3.12"-
      - name: Install Dependencies
        run: |
          python -m pip install uv hatch
          uv sync --locked --all-extras
          echo "$GITHUB_WORKSPACE/.venv/bin" >> $GITHUB_PATH

      - name: Run Coverage-Guided Fuzzing (1,000,000 iterations)
        run: pytest tests/core/test_fuzz.py -m fuzzing -v

      - name: Save Verification Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fuzzing-report
          path: fuzzing_report.json
          if-no-files-found: ignore

  # ------------------------------------------------------------------
  # JOB 3.7: VALIDATE EXTRAS
  # ------------------------------------------------------------------
  validate-extras:
    name: Validate Documented Extras
    needs: build
    if: needs.plan.outputs.matrix != '[]'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        extra: ["export", "duckdb", "mongodb", "neo4j", "snowflake", "cli"]
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Cache Workspace Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/uv
            .venv
          key: uv-venv-${{ runner.os }}-"3.10"-${{ hashFiles('uv.lock') }}
          restore-keys: |
            uv-venv-${{ runner.os }}-"3.10"-

      - name: Install hatch
        run: python -m pip install uv hatch

      - name: Tighten Internal Dependencies
        run: python scripts/tighten_dependencies.py --rewrite

      - name: Build Packages
        run: |
          (cd packages/plugins-workflows && hatch build)
          (cd packages/plugins-sinks && hatch build)
          (cd packages/core && hatch build)

      - name: Restore Internal Dependencies
        if: always()
        run: python scripts/tighten_dependencies.py --restore

      - name: Install Core with Extra
        run: |
          WHEEL=$(ls packages/core/dist/imednet-*.whl)
          SINKS_WHEEL=$(ls packages/plugins-sinks/dist/imednet_plugins_sinks-*.whl)
          WORKFLOWS_WHEEL=$(ls packages/plugins-workflows/dist/imednet_workflows-*.whl)
          python3 -m pip install "$WHEEL" "$SINKS_WHEEL" "$WORKFLOWS_WHEEL"
          python3 -m pip install "${WHEEL}[${{ matrix.extra }}]"

      - name: Verify CLI Availability
        run: |
          imednet --help || true

  # ------------------------------------------------------------------
  # JOB 3.8: VALIDATE PACKAGE CONSUMPTION
  # ------------------------------------------------------------------
  validate-package-consumption:
    name: Validate Package Consumption
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Cache Workspace Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/uv
            .venv
          key: uv-venv-${{ runner.os }}-"3.10"-${{ hashFiles('uv.lock') }}
          restore-keys: |
            uv-venv-${{ runner.os }}-"3.10"-

      - name: Install build tool
        run: python -m pip install build

      - name: Tighten Internal Dependencies
        run: python scripts/tighten_dependencies.py --rewrite

      - name: Validate Package Consumption
        run: python scripts/validate_package_consumption.py

      - name: Restore Internal Dependencies
        if: always()
        run: python scripts/tighten_dependencies.py --restore

  # ------------------------------------------------------------------
  # JOB 4: DOCS (Build & Prepare for Deploy)
  # ------------------------------------------------------------------
  docs:
    name: Build Documentation
    needs: plan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Cache Workspace Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/uv
            .venv
          key: uv-venv-${{ runner.os }}-"3.10"-${{ hashFiles('uv.lock') }}
          restore-keys: |
            uv-venv-${{ runner.os }}-"3.10"-

      - name: Install Dependencies
        run: |
          python -m pip install uv hatch
          uv sync --locked --all-extras
          echo "$GITHUB_WORKSPACE/.venv/bin" >> $GITHUB_PATH

      - name: Build HTML and Run Doctests
        run: hatch run docs

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/_build/html

  # ------------------------------------------------------------------
  # JOB 5: DEPLOY DOCS (Only on main push)
  # ------------------------------------------------------------------
  deploy-docs:
    name: Deploy to GH Pages
    needs: [plan, test, docs]
    # Only deploy docs if tests pass (or are skipped) and we are on main
    if: always() && !cancelled() && !contains(needs.*.result, 'failure') && github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4

  # ------------------------------------------------------------------
  # JOB 6: PUBLISH TO PYPI
  # release-please manifest mode creates tags of the form
  #   <package-name>-v<version>, e.g. imednet-v0.7.0
  # Publish jobs run on package-specific tags and on release commits to main
  # (`chore: release main`) so publication does not depend on tag push events.
  # `skip-existing` keeps reruns idempotent.
  # ------------------------------------------------------------------
  publish:
    name: Publish ${{ matrix.package }} to PyPI
    needs: [plan, test]
    if: |
      github.event_name == 'push' && (
        startsWith(github.ref, 'refs/tags/') ||
        (github.ref == 'refs/heads/main' && startsWith(github.event.head_commit.message, 'chore: release main'))
      ) && needs.plan.outputs.matrix != '[]'
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write # Mandatory for trusted publishing
    strategy:
      fail-fast: false
      matrix:
        package: ${{ fromJSON(needs.plan.outputs.matrix) }}
    steps:
      - name: Set Artifact Name
        id: set-artifact
        run: |
          if [ "${{ matrix.package }}" = "core" ]; then
            echo "artifact=dist-core" >> $GITHUB_OUTPUT
          elif [ "${{ matrix.package }}" = "plugins-workflows" ]; then
            echo "artifact=dist-workflows" >> $GITHUB_OUTPUT
          elif [ "${{ matrix.package }}" = "providers-airflow" ]; then
            echo "artifact=dist-providers" >> $GITHUB_OUTPUT
          elif [ "${{ matrix.package }}" = "plugins-sinks" ]; then
            echo "artifact=dist-sinks" >> $GITHUB_OUTPUT
          elif [ "${{ matrix.package }}" = "plugins-streamlit" ]; then
            echo "artifact=dist-streamlit" >> $GITHUB_OUTPUT
          fi

      - name: Download Artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: ${{ steps.set-artifact.outputs.artifact }}
          path: dist

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33 # release/v1
        with:
          skip-existing: true

  # ------------------------------------------------------------------
  # JOB X: ACCESSIBILITY AUDIT
  # ------------------------------------------------------------------

  # ------------------------------------------------------------------
  # JOB Y: VISUAL REGRESSION
  # ------------------------------------------------------------------
  visual-regression:
    name: Visual Regression Tests
    needs: plan
    if: needs.plan.outputs.matrix != '[]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Cache Workspace Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/uv
            .venv
          key: uv-venv-${{ runner.os }}-"3.12"-${{ hashFiles('uv.lock') }}
          restore-keys: |
            uv-venv-${{ runner.os }}-"3.12"-

      - name: Install Dependencies
        run: |
          python -m pip install uv hatch
          uv sync --locked --all-extras
          echo "$GITHUB_WORKSPACE/.venv/bin" >> $GITHUB_PATH
          hatch run playwright install --with-deps chromium

      - name: Run Visual Tests
        run: |
          hatch run pytest tests/browser/test_visual_regression.py

      - name: Upload Visual Diffs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: visual-diffs
          path: reports/visual_diffs/
          retention-days: 5
          if-no-files-found: ignore

  accessibility-audit:
    name: WCAG 2.1 Accessibility Audit
    needs: plan
    if: needs.plan.outputs.matrix != '[]'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Cache Workspace Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/uv
            .venv
          key: uv-venv-${{ runner.os }}-"3.12"-${{ hashFiles('uv.lock') }}
          restore-keys: |
            uv-venv-${{ runner.os }}-"3.12"-
      - name: Install Dependencies
        run: |
          python -m pip install uv hatch
          uv sync --locked --all-extras
          echo "$GITHUB_WORKSPACE/.venv/bin" >> $GITHUB_PATH
          hatch run playwright install --with-deps chromium
      - name: Run automated accessibility scan
        env:
          IMEDNET_VPAT_PATH: "docs/reference/VPAT.rst"
          IMEDNET_A11Y_REPORT_PATH: "packages/plugins-streamlit/src/imednet_streamlit/pages/a11y_report.json"
        run: |
          hatch run test -m a11y tests/browser/test_a11y.py

      - name: Auto-update VPAT
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          if ! git diff --quiet docs/reference/VPAT.rst packages/plugins-streamlit/src/imednet_streamlit/pages/a11y_report.json; then
            git config --local user.email "github-actions[bot]@users.noreply.github.com"
            git config --local user.name "github-actions[bot]"
            git add docs/reference/VPAT.rst packages/plugins-streamlit/src/imednet_streamlit/pages/a11y_report.json
            git commit -m "docs: auto-update VPAT and conformance report from latest accessibility audit"
            git push
          else
            echo "No VPAT or accessibility report changes to commit."
          fi
